<form method="post" action="{{ path('app_craftsman_delete', {'id': craftsman.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ craftsman.id) }}">
	<button class="btn btn-danger btn-sm mx-2">Delete</button>
</form>
